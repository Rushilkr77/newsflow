# Core
pydantic>=2.0
python-dotenv>=1.0

# AI / LLM
anthropic>=0.34
openai>=1.0          # used for Ollama OpenAI-compatible API (USE_LOCAL_LLM=true)
mcp>=1.0             # FastMCP server for article_fetcher_server.py (optional — server mode only)

# Gmail + Google Cloud TTS
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.1
google-cloud-texttospeech>=2.16

# HTML parsing
beautifulsoup4>=4.12
lxml>=5.0

# Article scraping
trafilatura>=1.8
newspaper3k>=0.2

# Embeddings (semantic dedup)
sentence-transformers>=2.7

# TTS (Phase 2 — Chatterbox primary + F5-TTS fallback + gTTS last resort)
chatterbox-tts>=0.1.0      # ResembleAI/Chatterbox — free local TTS (primary)
f5-tts>=0.9.0              # SWivid/F5-TTS — natural prosody fallback (free local)
gTTS>=2.5                  # Last-resort fallback
pydub>=0.25
pyloudnorm>=0.1.1
scipy>=1.11                # wav array conversion for Chatterbox/F5-TTS output
numpy>=1.24                # audio sample manipulation
noisereduce>=3.0           # spectral noise reduction for Chatterbox output

# Logging
structlog>=24.0

# Config
PyYAML>=6.0
