"""
Scheduler — Phase 3
Runs the pipeline at 5:00 AM IST daily via APScheduler.
Stub implementation for Phase 1.
"""
# TODO Phase 3: implement scheduler
# from apscheduler.schedulers.blocking import BlockingScheduler
# from apscheduler.triggers.cron import CronTrigger
# from orchestrator.pipeline import NewsFlowPipeline
#
# scheduler = BlockingScheduler(timezone="Asia/Kolkata")
# scheduler.add_job(NewsFlowPipeline().run, CronTrigger(hour=5, minute=0))
# scheduler.start()
